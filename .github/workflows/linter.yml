name: Lint codebase

"on":
  push:
    branches: ["**"]
    tags-ignore: ["**"]
  pull_request:
    branches: [main]
jobs:
  lint:
    name: Lint codebase (with pre-commit)
    runs-on: ubuntu-22.04
    steps:
      - name: Setup Python3
        uses: actions/setup-python@v3
        with:
          python-version: ${{ vars.PYTHON_VERSION }}
          cache: pip
      - name: Cache dependencies
        uses: actions/cache@v3
        with:
          path: |
            ~/.local/bin/hadolint
            ~/.local/bin/shfmt
          # yamllint disable-line rule:quoted-strings
          key: "hadolint-${{ vars.HADOLINT_VERSION }}-\
            shfmt-${{ vars.SHFMT_VERSION }}"
      - name: Install dependencies
        env:
          HADOLINT_VERSION: ${{ vars.HADOLINT_VERSION }}
          SHFMT_VERSION: ${{ vars.SHFMT_VERSION }}
        run: ./.github/scripts/setup-pre-commit.sh
        shell: bash
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Cache pre-commit
        uses: actions/cache@v3
        with:
          path: ~/.cache/pre-commit
          # yamllint disable-line rule:quoted-strings
          key: "${{ runner.os }}-python-${{ vars.PYTHON_VERSION }}-pre-commit-\
            ${{ hashFiles('.pre-commit-config.yaml') }}"
          restore-keys: |
            ${{ runner.os }}-python-${{ vars.PYTHON_VERSION }}-pre-commit-
      - name: Run pre-commit
        env:
          SKIP: notebooks-cells
        run: pre-commit run --color=always --all-files
        shell: bash
