CustomRainToday:
  speech:
    text: >-
      {% if (states('sensor.gw3000a_daily_rain_piezo') | float(0)) > 0 %}
        It rained {{ states('sensor.gw3000a_daily_rain_piezo') }} millimetre today.
      {% else %}
        There was no rain today...
      {% endif %}
