CustomHelloWorld:
  speech:
    text: >-
      Party-time in the {{ area_name(room) }}!
  async_action: true
  action:
    - action: light.turn_on
      data:
        entity_id: >-
          {{ room }}
        flash: long
